// Root entrypoints for directory-path loading (e.g. "plugins": ["/path/to/oc-terminal"]
// in opencode.json). npm/Git package installs resolve through package.json exports.
export { default } from "./dist/index.js"
